import { FormHero, HeroDiv, HeroTitle } from './Style'

const Hero = () => (
  <FormHero>
    <HeroDiv className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroDiv>
  </FormHero>
)

export default Hero
